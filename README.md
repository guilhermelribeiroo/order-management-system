# order-management-system

I'm coding this with AI just to test some agents

bla

If you want to add a new migration run: "dotnet ef migrations add migration_name --project ./Infrastructure --startup-project ./WebAPI"
